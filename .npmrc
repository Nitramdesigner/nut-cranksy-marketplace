touch .npmrc
```

### **Step 2 - Open `.npmrc` in VS Code**
- Look in the left sidebar
- You should see `.npmrc` at the root level
- Click to open it

### **Step 3 - Type this INSIDE the file:**
```
legacy-peer-deps=true